from program_3 import multiply_numbers

def sum_of_numbers(numbers):
    sum=0
    for n in numbers:
        sum=sum+n
    return sum
# n=sum_of_numbers([1,2,3,4,5]) 
# print(n)

print(multiply_numbers(10, 20))
